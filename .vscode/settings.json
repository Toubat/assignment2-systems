{
  // Triton import resolution lives in pyrightconfig.json (extraPaths -> .triton-stubs).
  // This file only sets editor conveniences.
  "python.defaultInterpreterPath": "${workspaceFolder}/.venv/bin/python",
  "editor.semanticHighlighting.enabled": true
}
